# Variáveis PÚBLICAS do Supabase (anon/publishable — seguras de expor, protegidas por RLS).
# Servem de padrão para o build funcionar de cara (login + navegação).
# Segredos (SUPABASE_SERVICE_ROLE_KEY, ANTHROPIC_API_KEY, IUGU_*) NÃO entram aqui —
# configure-os no painel da Vercel quando precisar.
NEXT_PUBLIC_SUPABASE_URL=https://tgmpaxcebiqonlcdnzbz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_C4SWrwpn_bfG3sXQU-C2mA_HgWmLNyE
