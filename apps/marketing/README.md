# @nexflow/marketing — Landing page pública (Sprint 4)

Next.js separado para `nexflow.com.br` (hero, pricing, demo, FAQ, SEO). Fica em
app próprio para deploy independente do `apps/web` (app autenticado em
`app.nexflow.com.br`). Esqueleto criado para fixar a estrutura do monorepo; a
implementação entra no Pilar 4 (Go-to-market).
